
const FilterItem = () => {

    return (
        <div className="filterbox">
            <form>
                <input type="checkbox" />
                <label>Filter beer search</label>
            </form>
        </div>
    );
};

export default FilterItem;